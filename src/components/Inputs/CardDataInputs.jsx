import { Input, Grid, Select, Form } from "antd";
import { getBrazilianStates } from "../../utils/estadosBrasileiros/states";
import PropTypes from "prop-types";

export function CardDataInputs({ formData, handleChange }) {
  return (
    <>
      <h1>Ola</h1>
    </>
  );
}
